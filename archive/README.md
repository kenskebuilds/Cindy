# Archive — superseded plans

Kept for reference only. **Nothing in this folder is the current plan.** The current
plan is the Deficit Hypertrophy Protocol at the repository root (`index.html`, built
from `src/protocol.html`).

## The Zero-Waste Protocol

- `zero-waste-protocol.html` — the standalone page as it was published.
- `zero-waste-protocol.src.html` — the source fragment it was built from.

Extracted on 29 Aug 2026 from commit `74b5c9a^`, the last commit before
`74b5c9a "Replace Zero-Waste Protocol with the Deficit Hypertrophy Protocol"`.
The files are byte-identical to that commit — to prove it:

    git show 74b5c9a^:index.html | diff - archive/zero-waste-protocol.html

### Why it was replaced

The Zero-Waste Protocol trained chest, biceps, glutes, rear delts, abs and both calf
heads **once a week**. The replacement brought every major muscle to the intended
frequency floor and named the four that deliberately remain 1× (rear delts, abs, front
delts, gluteus medius). See the full commit message on `74b5c9a` for the rest.

### What it is still useful for

Its citations and the reasoning behind doses that survived into the current plan. Where
the two documents disagree, **the current plan wins** — several numbers here were later
corrected, including the weekly failure-set count and the glute volume.
