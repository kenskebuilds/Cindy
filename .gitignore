# This repository is PUBLIC and serves GitHub Pages from its root.
# Anything committed here is world-readable. Never add secrets.
.env
.env.*
*.key
*.pem
secrets*
credentials*
api-key*

# Editor/script backups. This repo publishes from root - never publish stale copies.
*.bak
*.bak-*
*.bak.*

# Third-party research input, not part of the published site.
gemini_research/
